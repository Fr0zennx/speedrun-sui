{
  "name": "sui-garage-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^6.0.5"
  }
}
